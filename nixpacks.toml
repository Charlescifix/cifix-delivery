[phases.setup]
nixPkgs = ["python312", "cairo", "pango", "gdk-pixbuf", "libffi", "pkg-config", "fontconfig", "freetype", "gobject-introspection"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["echo Build phase completed"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"